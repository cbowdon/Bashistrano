## Host details
user='chris'

## remote_PATH :: PATH to set on remote machine. Defaults to the PATH SSH normally sets.
# remote_PATH='/usr/bin:/bin/'

## SSH command to execute. Defaults to 'ssh -tt'.
# ssh_cmd='ssh -tt'